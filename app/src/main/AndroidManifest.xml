<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:resizeableActivity="true"
        android:supportsPictureInPicture="true"
        android:theme="@style/Theme.MiniCompose">

        <!-- Process 1: Left GPU Activity (:left_gpu) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:process=":left_gpu"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:hardwareAccelerated="true"
            android:label="⚡ GPU (graphicsLayer)"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Process 2: Right CPU Activity & Service (:right_cpu) -->
        <activity
            android:name=".RightCpuActivity"
            android:exported="true"
            android:process=":right_cpu"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:hardwareAccelerated="true"
            android:label="⚠️ CPU (offset)"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".RightCpuService"
            android:process=":right_cpu"
            android:exported="false" />

    </application>

</manifest>
